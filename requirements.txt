# SmartTrading — Python Dependencies
# Install: pip install -r requirements.txt

yfinance>=0.2.40
pandas>=2.0.0
# pandas-ta tidak support Python 3.11 di Linux — diganti ta (technical analysis)
ta>=0.11.0
requests>=2.31.0
supabase>=2.3.0
python-dotenv>=1.0.0
# news_fetcher.py — RSS parsing pakai stdlib (xml.etree, email.utils)
# dateutil untuk robust RSS date parsing
python-dateutil>=2.9.0
